package sdk

// Note: gomobile/gobind only supports (T, error) patterns for exporting errors.
// A custom error wrapper object is not necessary and multi-return (T1, T2, err)
// signatures are not supported.
